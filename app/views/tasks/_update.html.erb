<div id="task_udpate_form">
  <%= form_for([@project, @task], remote: true) do |f| %>
    <div class="form-group">
      <%= f.text_area :title, class: "form-control", id: "task_title_update_field", remote: true %>

    <div class="form-group">
      <%= f.submit class: "btn btn-default" %>
    </div>
  <% end %>
</div>